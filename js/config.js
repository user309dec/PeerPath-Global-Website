/* ==========================================================================
   PeerPath Global — site-wide configuration
   Edit these values to update contact details across the whole website.
   ========================================================================== */
window.PEERPATH_CONFIG = {
  brandEn: "PeerPath Global",
  brandCn: "领途英语",
  wechatId: "PeerPathGlobal",            // 显示在联系/二维码区域的微信号
  email: "hello@peerpathglobal.com",     // 联系邮箱
  // 可选：用于接收测验提交的后端地址（如 Formspree / Google Apps Script / 自建 API）。
  // 留空则只在浏览器本地（localStorage）记录，方便早期试运营。
  submitEndpoint: ""
};
